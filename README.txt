This is a simple RPC system based on socket.

Server can regist the implemented function and store within server for client to invoke, limit for number of functions registered is dynamic.

Client is able to invoke the functions in server if function exist, server will return the result of calculation to client.

More informations please see answers.txt.
